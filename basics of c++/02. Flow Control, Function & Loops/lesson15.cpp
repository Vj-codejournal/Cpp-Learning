#include <iostream>

using namespace std;

int main(){
    for(int i =0; i != 5 ; i++); //as ; here become equivalent to {;} so nothing is inside loop now //as soon as become equal to 5 loop stop
        cout<<"a\n";
    
    return 0;
}